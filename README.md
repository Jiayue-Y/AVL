This program demonstrate a AVL with functions of insertion, deletion, search, and in-order Traversal. 
There are private helper functions for each of them, such as functions for rotations and balancing node.